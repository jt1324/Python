hig = float(input('Add the height: '))
wid = float(input('Add the width: '))
m2 = hig*wid
pai = m2/2
print('For the wall with {} high and {} width, or {} square meters, you will need {} liters of paint.'.format(hig, wid, m2, pai))